<tr>
  <td><%= blog.title %></td>
  <td><%= blog.body %></td>
  <td><%= link_to blog.status.to_s.slice(0,1).capitalize + blog.status.to_s.slice(1..-1), toggle_status_blog_path(blog) %></td>
  <td><%= link_to 'Show', blog %></td>
  <td><%= link_to 'Edit', edit_blog_path(blog) %></td>
  <td><%= link_to 'Delete', blog, method: :delete, data: { confirm: 'Are you sure want to delete this blog?' } %></td>
</tr>