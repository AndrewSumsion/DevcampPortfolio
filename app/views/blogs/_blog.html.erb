<tr>
  <td><%= blog.title %></td>
  <td>Published <%= distance_of_time_in_words(blog.created_at, Time.now) %> ago</td>
  <td><%= blog.body %></td>
  <td><%= link_to blog.status.to_s.slice(0,1).capitalize + blog.status.to_s.slice(1..-1), toggle_status_blog_path(blog) %></td>
  <td><%= link_to 'Show', blog %></td>
  <td><%= link_to 'Edit', edit_blog_path(blog) if logged_in?(:site_admin) %></td>
  <td><%= link_to 'Delete', blog, method: :delete, data: { confirm: 'Are you sure want to delete this blog?' } if logged_in?(:site_admin) %></td>
</tr>